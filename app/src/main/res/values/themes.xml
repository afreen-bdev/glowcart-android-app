<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.GlowCart" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/nykaa_pink</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/nykaa_pink_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/app_bg</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/nykaa_pink</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Optional: default font style -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/grey_text</item>
    </style>

</resources>
